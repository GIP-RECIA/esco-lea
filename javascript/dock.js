window.addEvent('load', function(){
	setDock();
})

function setDock(){	
	/* var dockElt = $('profils').getElements('a');
	dockElt.each(function(el){
		if (!el.hasClass($('profils').getProperty('class')))
		{
			var fx;
			var e = el.getElement('img');
			e.addClass('dock_min');
			el.addEvent('mouseover', function(){
				fx = new Fx.Styles(e, {duration: 250}).start({
				    'height': 40,
				    'width': 40
				});
			});
			el.addEvent('mouseout', function(){
				fx.stop();
				new Fx.Styles(e, {duration: 250}).start({
				    'height': 35,
				    'width': 35
				});
			});
		}
	})*/
}